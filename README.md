# Spline Next Demo

Simple Next.js demo showing a Spline scene.

Prerequisites:

- Node.js (16+)

Install and run (Windows PowerShell):

```powershell
npm install
npm run dev
```

Open http://localhost:3000 in your browser.
